apiVersion: v1
kind: Pod
metadata:
  name: "movie-service-test-connection"
  labels:
    helm.sh/chart: fastapiapp-0.1.0
    app.kubernetes.io/name: fastapiapp
    app.kubernetes.io/instance: RELEASE-NAME
    app.kubernetes.io/version: "latest"
    app.kubernetes.io/managed-by: Helm
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['movie-service:80/api/v1/checkapi']
  restartPolicy: Never
